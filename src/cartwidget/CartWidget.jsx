import React from 'react';

const CartWidget = () => {
  return (
    <image>
      <img
        className="cart-img"
        src={require('../../src/images/robotarm.png')}
        alt=""
      />
    </image>
  );
};

export default CartWidget;
